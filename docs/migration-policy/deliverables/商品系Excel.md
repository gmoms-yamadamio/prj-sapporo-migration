# 商品系 Excel 作成方針

> 全体方針: [作業方針設計書.md](../作業方針設計書.md)  
> 項目定義: [【サッポロビール様】CSVファイルIF設計書.xlsx](../【サッポロビール様】CSVファイルIF設計書.xlsx)

## 概要

`Products.csv`（3 行ヘッダ）を Excel に取り込み、関数で商品マスタ・販売基本情報・販売条件の 3 シートを生成する。

## Excel 構成

| シート | 役割 |
| --- | --- |
| Products | `input/raw/Products.csv` の取込（行 4 以降がデータ） |
| 商品マスタ | 関数により弊社指定フォーマットへ変換 |
| 販売基本情報 | 同上 |
| 販売条件 | 同上 |
| マスタ・変換表 | サイト ID、管理グループコード等 |

## Products.csv 列定義（英語ヘッダ行）

| 列名 | 日本語 | 型 | 用途 |
| --- | --- | --- | --- |
| Id | 内部商品ID | Int32 | 旧サイト商品主キー。注文明細の productId と対応 |
| ExternalId1 | 外部ID1 | String | **SKUコード / 商品コード** として使用 |
| ExternalId2 | 外部ID2 | String | 予備 |
| Name | 管理名 | String | 商品名 |
| SalesStatus | 販売ステータス | Int32 | 販売可否 |
| SalesStart | 販売開始日時 | DateTime | |
| SalesEnd | 販売終了日時 | DateTime | |
| Stockout | 在庫0 | Boolean | |
| UnitPrice | 単価 | Decimal | 販売価格 |
| SalesPatternId | 販売パターンID | Int32 | 販売条件の識別 |

> **2026-07-09 更新**: 【サッポロビール様】CSVファイルIF設計書.xlsx の「商品マスタ CSV 項目」「販売基本情報 CSV 項目」「販売条件 CSV 項目」「サンプル」各シートを精査し、固定値・必須属性・サンプル値を反映した。以下のマッピングは「暫定」から「IF 設計書ベースの確定値＋要確認事項」に更新している。

## 商品マスタ マッピング（IF 設計書反映）

| 商品マスタ CSV 項目 | 必須(新規/更新/削除) | Products 列 / 固定値 | 備考 |
| --- | --- | --- | --- |
| 操作区分 | 必須/必須/必須 | 固定 `C` | |
| 商品マスタコード | 必須/必須/必須※ | `ExternalId1` に `mig_` プレフィックスを付与 | **プレフィックスは `mig_`（確定）**。例: `mig_sku00001` |
| 商品マスタ名 | 必須/〇/× | `Name` | |
| 管理グループコード | 必須/×/× | 固定 `sp_common` | **修正**。従来 `shupark` としていたが、IF 設計書・サンプル行とも商品系 3 CSV（商品マスタ／販売基本情報／販売条件）はすべて `sp_common` 固定（会員・注文系の `shupark` とは異なる区分。[作業方針設計書.md](../作業方針設計書.md) §9 参照） |
| バリエーションコード1〜3 | 〇/〇/× | 未設定 | 単一 SKU 想定のため使用しない。設定なし時は項目削除または `_NULL` |
| 販売基本情報作成フラグ | 必須/×/× | 固定 `0` | 「0：販売基本情報を同時作成しない」。販売基本情報 CSV を別途投入するため `0` 固定 |
| ソート順 | 〇/〇/× | 未設定 | `0`（指定なし）でも項目削除でも可 |

## 販売基本情報 マッピング（IF 設計書反映）

| 販売基本情報 CSV 項目 | 必須(新規/更新) | 入力元 / 固定値 | 備考 |
| --- | --- | --- | --- |
| 操作区分 | 必須/必須 | 固定 `C` | |
| 販売基本情報コード | 必須/必須 | 商品マスタコードと同一値 | |
| 商品名 | 必須/〇 | `Name` | |
| 販売停止 | 必須/〇 | 固定 `1`（販売停止） | **移行商品は一律 `1`（確定）**。OPEN 後の販売商品は別途登録 |
| 非表示 | 必須/〇 | 固定 `1`（非表示） | 同上（一律 `1` 確定） |
| 管理グループコード | 必須/× | 固定 `sp_common` | |
| 掲載サイトID | 必須/必須 | 固定 `2`（shupark） | **`2`（shupark）で確定**。`\|` 区切りで複数指定可 |
| [カスタム]酒類フラグ | 必須/必須 | 固定 `対象外` | **全商品一律 `対象外`（確定）**。判定は行わない |
| バリエーションコード1 | 〇/× | 未設定 | |
| [カスタム]商品タグ | 〇/〇 | 未設定（任意） | `\|` 区切り。候補値: 送料無料／新着／定期便／数量限定商品 |
| [カスタム]ブランド名 | 〇/〇 | `ExternalId2` または未設定 | 登録しない場合は項目削除 |
| [カスタム]トップページ用商品概要 / [カスタム]商品概要 | 〇/〇 | 未設定 | 登録しない場合は項目削除 |
| ソート順 / 説明文1 / 説明文2 | 〇/〇 | 未設定 | |

## 販売条件 マッピング（IF 設計書反映）

| 販売条件 CSV 項目 | 必須(新規/更新/削除) | 入力元 / 固定値 | 備考 |
| --- | --- | --- | --- |
| 操作区分 | 必須/必須/必須 | 固定 `C` | |
| 管理グループコード | 必須/×/× | 固定 `sp_common` | |
| 販売基本情報コード | 必須/×/× | 商品マスタコードと同一値 | |
| 販売条件コード | 必須/×/× | 商品マスタコードと同一値 | |
| 販売SKUコード | 必須/〇/× | 商品マスタコードと同一値 | 単一 SKU 想定 |
| 非表示 | 〇/〇/× | 固定 `1`（非表示） | 商品マスタ・販売基本情報と合わせ一律 `1`（確定） |
| 配送方法グループコード | 必須/〇/× | 固定 `migration-delivery-group` | データ移行用の配送グループコード |
| 決済方法グループコード | 必須/〇/× | 固定 `migration-pay-group` | データ移行用の決済方法グループコード |
| 表示対象ユーザー | 必須/〇/× | 固定 `1`（全員に表示） | |
| 配送温度帯ID | 必須/〇/× | 固定 `T1111` | **`T1111` で確定**（IF 設計書「サンプル」タブの実例行に合わせる。[注文履歴CSV.md](./注文履歴CSV.md) の配送温度帯ID とも統一） |
| 販売価格 | 必須/〇/× | `UnitPrice` | 0 以上の正数 |
| 税率(販売価格) | 必須/〇/× | `Products.csv` の税率列（受領後）／暫定 `10` | **軽減税率対象商品（8%）が存在するため一律 `10` は不可**。`Products.csv` に税率列を含めてもらうようお客様調整中（[business-rules-confirmation.md](../business-rules-confirmation.md) #21） |
| 販売基本情報ID | 必須※/×/× | 販売SKUコードで代替可 | 「販売基本情報コード＋バリエーション」または「販売SKUコード」のいずれかが必須 |
| その他（表示優先順・入荷通知メール非対応・発売日・出荷予定日・購入可能数 等） | 〇/〇/× | 未設定 | 本移行では設定不要。デフォルト（無制限・指定なし）で登録 |

## カスタム項目・管理グループ（確定・要確認まとめ）

| 項目 | 値 | ステータス |
| --- | --- | --- |
| 管理グループコード（商品系 3 CSV 共通） | `sp_common` | 確定（IF 設計書・サンプルで一致） |
| 管理グループコード（会員・注文 CSV） | `shupark` | 確定（[作業方針設計書.md](../作業方針設計書.md) §9） |
| 掲載サイト ID | 固定 `2`（shupark） | 確定 |
| 酒類フラグ | 固定 `対象外` | 確定（全商品一律 `対象外`） |
| 商品マスタコードのプレフィックス | `mig_` | 確定 |
| 配送温度帯ID | `T1111` | 確定 |
| 販売停止・非表示のデフォルト値 | 一律 `1`（販売停止・非表示） | 確定（OPEN 後の販売商品は別途登録） |
| 税率(販売価格) | `Products.csv` 税率列（受領後） | 軽減税率商品あり。税率列の受領をお客様調整中 |
| ブランド名 | `ExternalId2` または手動付与 | 暫定 |

## CSV エクスポート手順

1. Excel で 3 シート（商品マスタ / 販売基本情報 / 販売条件）をそれぞれ CSV 形式で保存
2. 文字コード UTF-8（BOM 付き推奨）
3. CEC 管理画面 → 各インポート画面からアップロード
4. インポート順: 商品マスタ → 販売基本情報 → 販売条件

## テンプレート生成

`scripts/generate_product_reference.py` が `Products.csv` から SKU 参照表（`output/products/product_sku_reference.csv`）を生成する。  
Excel テンプレート本体は IF 設計書のサンプル列構成に合わせて手動作成し、上記関数定義を埋め込む。

## 改訂履歴

| 日付 | 内容 |
| --- | --- |
| 2026-07-01 | 初版。Products 列マッピング・エクスポート手順を定義 |
| 2026-07-09 | IF 設計書「商品マスタ／販売基本情報／販売条件 CSV 項目」シートおよび「サンプル」タブを反映。管理グループコードを `sp_common` に修正（従来 `shupark` は誤り）。固定値項目（配送方法／決済方法グループコード等）を追加。プレフィックス・配送温度帯ID・非表示デフォルト値等の表記不一致を要確認事項として明記 |
| 2026-07-14 | お客様回答を反映。商品マスタコードのプレフィックス `mig_`、掲載サイトID `2`、酒類フラグ一律 `対象外`、販売停止・非表示 一律 `1`、配送温度帯ID `T1111` を確定。税率は軽減税率商品が存在するため `Products.csv` への税率列追加をお客様調整中 |
